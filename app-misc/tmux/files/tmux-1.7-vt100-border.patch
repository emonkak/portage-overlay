diff -ur tmux-1.7.orig/tty-acs.c tmux-1.7/tty-acs.c
--- tmux-1.7.orig/tty-acs.c     2012-10-12 01:51:06.000000000 +0900
+++ tmux-1.7/tty-acs.c  2012-10-27 18:28:40.606614082 +0900
@@ -81,7 +81,7 @@
 	struct tty_acs_entry *entry;
 
 	/* If not a UTF-8 terminal, use the ACS set. */
-	if (!(tty->flags & TTY_UTF8)) {
+	if (1) {
 		if (tty->term->acs[ch][0] == '\0')
 			return (NULL);
 		return (&tty->term->acs[ch][0]);
diff -ur tmux-1.7.orig/tty.c tmux-1.7/tty.c
--- tmux-1.7.orig/tty.c 2012-10-12 01:51:06.000000000 +0900
+++ tmux-1.7/tty.c      2012-10-27 18:29:06.280518550 +0900
@@ -52,7 +52,7 @@
 	    const struct grid_cell *, const struct grid_utf8 *);
 
 #define tty_use_acs(tty) \
-	(tty_term_has((tty)->term, TTYC_ACSC) && !((tty)->flags & TTY_UTF8))
+	(tty_term_has((tty)->term, TTYC_ACSC))
 
 #define tty_pane_full_width(tty, ctx) \
 	((ctx)->xoff == 0 && screen_size_x((ctx)->wp->screen) >= (tty)->sx)
